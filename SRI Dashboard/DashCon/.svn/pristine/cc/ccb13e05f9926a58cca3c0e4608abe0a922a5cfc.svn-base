/*global require*/
'use strict';

require.config({
    shim: {
    	jquery: {
    		exports: '$'
    	},
        bootstrap: {
            deps: ['jquery'],
            exports: 'jquery'
        },
        angular: {
        	exports: 'angular'
        }
        'angular-ui-router': {
        	exports: 'ui-router'
        }
    },
    paths: {
        jquery: '../../js/jquery-1.11.0',
        angular: '../../vendor/angular/angular',
        'angular-ui-router' : '../../vendor/angular-ui-router/angular-ui-router'
    }
});

require([], function() {
	
	//added function to capitalize first letters
	String.prototype.capitalize = function() {
    return this.charAt(0).toUpperCase() + this.slice(1);
	};
	
});

require([
    'jquery',
    'angular',
    'angular-ui-router'
], function () {
	
	// Initialize our app's namespace
	var sri = sri || {};
    
    console.log('Smart Roadside Admin Initiated!');
    
});

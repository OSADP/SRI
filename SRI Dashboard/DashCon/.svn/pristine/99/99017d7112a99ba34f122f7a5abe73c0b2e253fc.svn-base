/**
 * @author rothrob
 * @define App, Backbone
 * @desc   Users Collection
 */
define(['backbone', 'models/User'], function(Backbone, User) {
	
	console.log(User);
	
	var Users = Backbone.Collection.extend({
		model: User,
		url: '/DashCon/resources/user',
		parse: function(response) {
			var users = response.user;
			this.reset();
			return users;
		}
	});
	
	return Users;
	
});
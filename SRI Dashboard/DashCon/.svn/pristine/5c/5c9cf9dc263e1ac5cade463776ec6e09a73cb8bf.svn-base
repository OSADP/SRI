
<script type="text/template" id="module-safer">
						
	<div class="col-xs-12 module-wrapper" id="safer">
		
		<div class="module-box module-safer">
	
			<div class="module-menu">
				
				<button id="btnSettings" class="btn btn-sm btn-inverse btn-clipboard " title="Settings">
					<span class="glyphicon glyphicon-cog"></span>
				</button>

				<button id="btnRefresh" class="btn btn-sm btn-inverse btn-clipboard " title="Refresh">
					<span class="glyphicon glyphicon-refresh"></span>
				</button>

				<a href="https://safer.fmcsa.dot.gov/" target="blank"
				 class="btn btn-sm btn-inverse btn-clipboard " title="Go To Safer">
					<span class="glyphicon glyphicon-new-window"></span> OPEN SAFER
				</a>

			</div>
			
			<h4>USDOT SAFER:</h4>
			
			<div class="well">
				<p>Name: <strong><span class="text-info pull-right">{{ legalName }}</strong></span></strong></p>
				<p>Database Administrator: <strong><span class="text-info pull-right">{{ dbaName }}</span></strong></p>
				<p>Physical Address: <strong><span class="text-info pull-right">{{ physicalStreet1 }} {{ physicalCity }}, {{ physicalState }} {{ physicalZip }}</span></strong></p>
				<p>Mailing Address: <strong><span class="text-info pull-right">{{ mailingStreet1 }} {{ mailingCity }}, {{ mailingState }} {{ mailingZip }}</span></strong></p>
				<p>US DOT #: <strong><span class="text-info pull-right">{{ usDotNumber }}</span></strong></p>
				<p>MC/MX: <strong><span class="text-info pull-right">{{ mcMx }}</span></strong></p>
				<p>Phone: <strong><span class="text-info pull-right">{{ phone }}</span></strong></p>
				<p>Fax: <strong><span class="text-info pull-right">{{ fax }}</span></strong></p>
				<p>Email: <strong><span class="text-info pull-right">{{ email }}</span></strong></p>
				<p>DOT Status: <strong><span class="text-info pull-right">{{ dotStatus }}</span></strong></p>
				<p>Entity: <strong><span class="text-info pull-right">{{ entity }}</span></strong></p>
				<p>Inspection Value: <strong><span class="text-info pull-right">{{ inspectionValue }}</span></strong></p>
			</div>

		</div>
		
	</div>
	
</script>